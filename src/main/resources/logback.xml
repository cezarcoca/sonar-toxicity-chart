<?xml version="1.0" encoding="UTF-8" ?>

<configuration debug="false">

  <!-- appender used to profile Sonar Server -->
  <appender name="PLUGINS_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <File>d:/plugins.log</File>
    <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
      <param name="FileNamePattern" value="../../logs/plugins.%i.log"/>
      <param name="MinIndex" value="1"/>
      <param name="MaxIndex" value="3"/>
    </rollingPolicy>
    <triggeringPolicy class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy">
      <param name="MaxFileSize" value="5MB"/>
    </triggeringPolicy>
    <layout class="ch.qos.logback.classic.PatternLayout">
      <pattern>
        %d{yyyy.MM.dd HH:mm:ss} %-5level %logger{20} %X -  %msg%n
      </pattern>
    </layout>
  </appender>

  <logger name="org.sonar.plugins.toxicity" >
    <level value="DEBUG"/>
    <appender-ref ref="PLUGINS_FILE"/>
  </logger>
    
  <root>
    <level value="WARN"/>
    <appender-ref ref="PLUGINS_FILE"/>
  </root>

</configuration>